@model IEnumerable<WKSADB.DivisionType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsGrandChampionDivision)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsGrandChampionDivision)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DivisionTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DivisionTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DivisionTypeId })
        </td>
    </tr>
}

</table>
